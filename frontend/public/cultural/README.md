# Cultural images

Drop static design imagery here. The homepage references these by exact filename
and shows each one only if the file is present (otherwise it's gracefully hidden).

| File | Used as | Suggested |
|------|---------|-----------|
| `bharat-mata.jpg` | Devotion panel on the homepage (portrait) | the Bharat Mata painting |
| `veena.png` | Centerpiece above the Instruments band (wide; transparent PNG looks best) | the Saraswati veena photo |

After adding/replacing a file, just refresh the page — no rebuild needed in dev.
Keep images reasonably sized (e.g. ≤ 500 KB) for fast loads.
